'use strict';

const Compra = () => {
    
    const section = $('<section id="datos" class="container"></section>');
    const row1 = $('<div class="row"></div>');
    const title = $(' <div class="col-sm-12 azul"><h2 class="omnes-regular">Ingresa tu información y compra en línea</h2><h1 class="omnes-semibold">TU PLAN A MEDIDA</h1></div>');

    const col = $('<div class="col-sm-12 "></div>');
    const div = $('<div class="detalle"></div>');
    const container = $('<div class="container"></div>');
    const row = $('<div class="row"></div>');

    

}

